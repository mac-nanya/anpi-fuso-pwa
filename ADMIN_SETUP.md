# 管理者による削除・復元の設定

実装済みの画面を実際のSupabaseで使うための初回設定です。
管理者は `mac.nanya@gmail.com` の1人です。パスワードはコードやチャットに記載しません。

## 1. データと権限の移行

SupabaseのSQL Editorで `supabase/migrations/20260925_admin_reports.sql` の内容を実行します。
既存の `schema.sql` は実行済みなので、再実行しないでください。
移行SQLは1回だけ実行します。全体をトランザクションで実行し、失敗した場合は変更を取り消します。

この移行は入力者名の空白と大文字・小文字を正規化し、同じ名前の最新回答を1件選びます。
古い回答は `superseded_by` を付けて保存したまま通常表示から外します。古い回答はごみ箱の復元対象にも含めません。
重複名は同じ入力者として扱います。同姓同名の人は、区別できる入力者名を使ってください。

移行後は旧バージョンのアプリからの読み書きが拒否されます。
公開サイトを切り替える際は、移行SQLの実行と新版の公開を続けて行い、利用者に再読み込みを案内してください。
旧版で失敗した未送信データは端末に残り、新版から再送されます（削除済みの回答は再送で復活しません）。

## 2. 管理者の作成

1. Supabaseの **Authentication → Users → Add user → Create new user** を開きます。
2. メールアドレスに `mac.nanya@gmail.com`、ご自身で決めたパスワードを入力します。
3. **Auto Confirm User** を有効にして作成します。既に同じユーザーがあれば再作成は不要です。
4. SQL Editorで `supabase/set_admin.sql` の内容を実行します。

管理者の指定はDB内のユーザーIDで行います。同じメールアドレスを入力しただけでは権限は得られません。
`private.report_admin` は管理者1人だけを保持します。ブラウザーからの参照・書換えは許可していません。
通常利用者向けのアカウント作成画面はありません。

## 3. アプリで確認

1. アプリを再読み込みし、「同期済み」になることを確認します。
2. 画面下の「管理者ログイン」を開き、メールアドレスとパスワードを入力します。
3. テスト用回答の詳細画面を開き、「この回答を削除する」を押します。
4. 名前・報告日時を確認して実行します。リストと集計から消えます。
5. 「管理者メニュー」→「ごみ箱を表示・更新」→「復元」で戻します。
6. ログアウトすると削除・復元の操作が消えることを確認します。
7. 別の未ログインのブラウザーで、30秒ごとの同期で削除・復元が反映されることを確認します。

ログイン状態はメモリー内だけに保持します。画面の再読み込み・タブを閉じた後は再ログインが必要です。
削除と復元には通信が必要です。未送信の変更がある回答は、同期完了後に削除してください。
確認した後に他の人が回答を更新した場合、削除・復元は中止し、最新の内容の確認を求めます。

## 動作と検証

- 通常の入力・修正はログイン不要。同じ名前の新規入力も、既存の1件を更新します。
- 削除は回答を残して `deleted_at` を設定します。同じ名前での再入力も、管理者が復元するまで拒否します。
- 公開APIは削除済み・旧回答の本文を返さず、IDと削除状態を差分同期に返します。
- 既に端末に保存された内容まで遠隔消去する機能ではありません。通信が復帰して同期すると表示から除外されます。
- 一般利用者はテーブルを直接更新できません。通常保存は公開RPC、削除・復元は管理者専用RPCで権限を確認します。
- `npm test` はPGliteのPostgreSQL環境で移行・権限・削除・復元・古い回答の再送拒否を検証します。
- ブラウザーテストの初回準備は `npx playwright install chromium`。既存Chromeを使う場合は `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/usr/bin/google-chrome npm run test:browser` でも実行できます。
- `npm run test:browser` は認証とAPIを模擬して画面操作を検証します。実際のSupabaseのアカウント設定や通信の確認は上記手順で行ってください。
