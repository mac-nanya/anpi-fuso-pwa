# サーバー同期

SupabaseのSQL Editorで `supabase/schema.sql` を初回だけ実行してください。
接続先と公開用Publishable keyは `.env.production` に設定済みです。
秘密キー・service_roleキー・データベースパスワードは設定しないでください。
ローカルでは `.env.example` を `.env.local` にコピーし、値を設定します。
環境変数の変更は再ビルドで反映されます。

## 動作

- 誰でも閲覧・登録・修正できます。削除は許可していません。
- 提出は端末に保存してから同じIDでサーバーへ送信します。再送で行が重複しません。
- 起動時、提出時、通信復帰時、画面に戻ったとき、30秒ごとに同期します。
- 通信失敗時は未送信データを保持し、再読込後も再送します。「今すぐ同期」でも再試行できます。
- 同期中の編集は未送信のまま保持します。
- 同じ回答の競合は最後にサーバーが受信した内容を採用します。古いオフライン編集も後から届けば上書きします。
- 初期サンプルは表示・自動送信しません。旧版の端末内未送信データは引き継ぎます。
- 未設定時は端末保存のみ。自動再送はアプリを開いている間に動作します。
- オフライン起動には更新後のアプリを一度オンラインで開く必要があります。

## 検証と公開

`npm test`、`npm run build` を実行します。
mainへ反映すると既存のGitHub ActionsでGitHub Pagesに公開されます。
実接続では、ブラウザーAで提出し、別のブラウザーBで同期・修正できることを確認します。
Aを圏外にして修正・再読込し、通信復帰後にBにも反映されることを確認してください。
